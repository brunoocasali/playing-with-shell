# There are my github aliases to improve my typing every day!

# Git Aliases

# Run these commands to use only inside current branch
# $ git config --global push.default current
# $ git config --global pull.default current

alias gp='git push'
alias gpl='git pull'
alias gc='git commit -m'
alias ga='git add .'
alias gst='git status'
alias gb='git branch'
